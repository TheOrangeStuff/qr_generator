version: "3.8"

services:
  qr-generator:
    build: .
    container_name: qr-generator
    ports:
      - "8080:5000"
    volumes:
      - qr_data:/data
    restart: unless-stopped
    environment:
      - DATA_DIR=/data

volumes:
  qr_data:
